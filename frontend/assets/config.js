/** Workbench runtime flags — AUTO_APPROVE off by default for human review. */
window.WORKBENCH_CONFIG = Object.freeze({
  AUTO_APPROVE: false,
  dryRunAutoApprove: false,
  apiMode: "dry-run",
  mockDataLabel: "MOCK / dry-run — 非真实译文",
});
