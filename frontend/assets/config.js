/** Workbench runtime flags — AUTO_APPROVE enabled for autonomous trial runs. */
window.WORKBENCH_CONFIG = Object.freeze({
  AUTO_APPROVE: true,
  dryRunAutoApprove: true,
  apiMode: "dry-run",
  mockDataLabel: "MOCK / dry-run — 非真实译文",
});
