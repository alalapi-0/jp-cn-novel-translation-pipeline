# workspace/review

本地质量审核报告输出目录（**gitignore**）。Round 49 CLI：

```bash
python3 scripts/run_quality_review.py --workspace
```

写入 `issue_report.json`。提交到 Git 的样例请使用 `data/examples/review_issue_report.example.json`。
